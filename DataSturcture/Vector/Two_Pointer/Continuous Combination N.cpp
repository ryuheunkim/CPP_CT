#include <iostream>
using namespace std;

int main() {
    // 로그 출력을 위해 입출력 가속 코드는 잠시 주석 처리하거나 뺍니다.
    // ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

    int N;
    cout << "숫자 N을 입력하세요 (예: 15): ";
    cin >> N;
    cout << "--------------------------------------------------" << endl;

    int start_index = 1;
    int end_index = 1; 
    int sum = 1;
    int count = 1; // N 자체(자기 자신)도 포함하므로 1부터 시작
    
    while (end_index < N){
        // 현재 상태 출력
        cout << "[상태] 범위: " << start_index << " ~ " << end_index 
             << " | 현재 합(sum): " << sum << endl;

        if (sum == N){
            cout << "  🎉 [정답!] 합이 " << N << "과 같습니다. (Count 증가)" << endl;
            cout << "  ➡️ [이동] 다른 경우도 찾기 위해 오른쪽(end)을 한 칸 늘립니다." << endl;
            
            count++;
            end_index++;
            sum = sum + end_index;
        }

        else if(sum > N) {
            cout << "  📉 [초과] 합이 " << N << "보다 큽니다(" << sum << "). 줄여야 합니다." << endl;
            cout << "  ➡️ [이동] 왼쪽(start) 값을 빼고, start를 한 칸 오른쪽으로 당깁니다." << endl;
            
            sum = sum - start_index; // 현재 start 값을 합에서 빼고
            start_index++;           // start를 오른쪽으로 이동
        }

        else { // sum < N
            cout << "  📈 [미달] 합이 " << N << "보다 작습니다(" << sum << "). 더 더해야 합니다." << endl;
            cout << "  ➡️ [이동] 오른쪽(end)을 한 칸 늘려 다음 숫자를 더합니다." << endl;
            
            // [수정된 부분] end를 먼저 늘리고, 그 늘어난 숫자를 더해야 합니다.
            end_index++; 
            sum = sum + end_index;
        }
        cout << "--------------------------------------------------" << endl;
    }

    cout << "최종 경우의 수(count): " << count << endl;
}